# 循环依赖

一个对象依赖对象闭环到自己

> A -> B -> .... ->A

tip:

> 不涉及代理对象问题

先把这几个词放到 **Spring Bean 创建流程** 里理解，不是普通 Java 对象生命周期的完整说法：

```text
实例化 Bean：
  Spring 根据 BeanDefinition 创建出 A 这个 Java 对象。
  底层可能是反射调用构造器、工厂方法或 CGLIB。
  到这一步，A 对象已经存在，但 A.b 还没有注入。

属性填充：
  Spring 给已经实例化出来的 Bean 设置依赖属性。
  比如给 A.b 注入 B，给 B.a 注入 A。

初始化 Bean：
  Spring 在属性填充之后，继续执行 Bean 生命周期回调。
  比如 Aware、BeanPostProcessor、afterPropertiesSet、init-method 等。
```

所以，Spring 里的一个完整单例 Bean，大致要经过：

```text
createBeanInstance()
  -> 实例化 Bean：A 这个 Java 对象被创建出来

populateBean()
  -> 属性填充：A.b 被注入

initializeBean()
  -> 初始化 Bean：执行 Spring 的初始化回调
```

结合下面 A、B 的例子，循环依赖卡住的位置是：

```text
1. Spring 开始创建 A。
   A 已经实例化出来了，但还没执行属性填充，所以 A.b 还没有值。

2. Spring 准备给 A.b 注入 B，于是开始创建 B。

3. B 也已经实例化出来了，但 B 在属性填充时又需要 A。

4. 这时 Spring 发现 A 正在创建中，于是从三级缓存拿到 A 的早期引用，先注入给 B.a。
   这里拿到的 A 不是完整初始化后的 Bean，而是“已经实例化、但还没完成属性填充和初始化”的早期引用。

5. B 拿到 A 的早期引用后，可以继续完成属性填充和初始化。

6. B 创建完成后，Spring 回到 A 的创建流程，把完整的 B 注入给 A.b。
   然后 A 继续完成初始化。
```

所以这篇文章后面说“提前暴露对象”，主要指的是：

```text
A 实例化后、属性填充完成前，Spring 先把 A 的早期引用暴露出去。
B 回头依赖 A 时，先拿这个早期引用完成 B.a 的注入。
```

## 先建立边界：本文只讲单例属性注入循环依赖

> [!note] 先限定问题范围
> 本文讲的是 Spring 单例 Bean 的属性注入循环依赖。
> 典型场景是：A 通过属性依赖 B，B 又通过属性依赖 A。
> Spring 能处理这类问题，是因为 A 实例化后、属性填充前，可以先把 A 的早期引用暴露出去。

这篇文章先不要泛化到所有循环依赖。边界可以这样记：

```text
本文重点：
  singleton Bean
  属性注入 / setter 注入
  A -> B -> A
  三级缓存如何拿到早期引用

本文不展开：
  prototype 循环依赖
  构造器循环依赖
  AOP 代理参与循环依赖的复杂情况
```

它在整个 Bean 创建主线里的位置是：

```text
doCreateBean()
  -> createBeanInstance()
  -> addSingletonFactory()
  -> populateBean()
     -> applyPropertyValues()
        -> resolveReference()
           -> getBean(refName)
              -> getSingleton()
```

也就是说，循环依赖不是一个独立流程，它嵌在 [[4、依赖注入(DI)]] 的 Bean 创建和属性填充过程中。

## 为什么本文只讲单例属性注入循环依赖

这里说“本文只讲单例属性注入循环依赖”，不是说其他循环依赖不存在，而是因为 Spring 最经典的三级缓存循环依赖，主要解决的是这个场景：

```text
单例 Bean
属性注入 / setter 注入 / 字段注入
A -> B -> A
```

比如：

```java
@Service
public class A {
    @Autowired
    private B b;
}

@Service
public class B {
    @Autowired
    private A a;
}
```

这种写法在 Spring 默认单例 Bean 场景下，通常是可以被解决的。

原因是 Spring 创建 Bean 不是一步完成的，而是大致分成：

```text
实例化 A
提前暴露 A 的早期引用
填充 A 的属性
初始化 A
```

所以 A 还没完整创建完时，B 如果需要 A，可以先拿到 A 的早期引用。

> [!note] 关键点
> Spring 能解决这类循环依赖的前提是：A 至少已经实例化出来了。
> 也就是说，Spring 得先有一个 A 对象，才能把 A 的早期引用提前暴露出去。

## 其他循环依赖分别是什么情况

不是所有循环依赖都能解决。

| 场景 | Spring 默认能不能解决 | 原因 |
|---|---|---|
| 单例 + 属性注入 / setter 注入 | 通常可以 | A 可以先实例化，再注入属性，存在提前暴露 A 的机会 |
| 单例 + 字段注入 `@Autowired` / `@Resource` | 通常可以 | 字段注入也发生在实例化之后、初始化之前，本质上属于属性填充阶段 |
| 单例 + 构造器注入 | 通常不行 | A 构造器需要 B，B 构造器又需要 A，A 还没创建出来，没法提前暴露 |
| prototype + 循环依赖 | 通常不行 | prototype 不走单例三级缓存，每次都是新对象，没有稳定的早期单例引用 |
| AOP / 事务代理参与循环依赖 | 比较复杂 | 提前暴露的可能不是原始对象，而是代理对象 |

所以，结论不是“不能写 A 里面注入 B，B 里面注入 A”。

更准确地说：

```text
Spring 可以解决一部分循环依赖；
但不是所有循环依赖都能解决；
业务上也不推荐主动设计循环依赖。
```

## Spring 能解决为什么还不推荐

因为“能解决”不等于“设计合理”。

如果业务代码里出现：

```text
A 依赖 B
B 又依赖 A
```

通常说明两个类的职责边界不够清楚。

更好的方向一般是：

```text
A -> B
B 不再反向依赖 A
```

或者抽出第三个协作者：

```text
A -> C
B -> C
```

所以 Spring 支持循环依赖，更多是为了兼容现实项目和历史代码，而不是鼓励业务代码这么设计。

## 支持循环依赖和三级缓存是什么关系

Spring 解决单例属性循环依赖，用的就是三级缓存机制。

但是要分清两个问题：

```text
循环依赖要解决的问题：
  B 需要 A 时，A 还没创建完，能不能先给 B 一个 A 的早期引用？

AOP / 事务代理带来的问题：
  这个早期引用到底应该是原始 A，还是 A 的代理对象？
```

如果没有 AOP、事务代理这些复杂情况，普通循环依赖只需要“提前暴露 A”。

但如果 A 是一个需要被代理的 Bean，比如有事务：

```java
@Transactional
public void doSomething() {}
```

那么最终容器里的 A 可能不是原始 A，而是代理对象。

这时 B 依赖 A，B 应该拿到的是代理 A，而不是原始 A。

否则可能出现：

```text
容器最终暴露的是代理 A
但 B 里面注入的是原始 A
```

这样事务、AOP 等能力就可能失效。

## 三级缓存分别解决什么

```text
一级缓存 singletonObjects：
  存放完整创建好的单例 Bean。

二级缓存 earlySingletonObjects：
  存放已经被提前拿出来用过的早期引用。

三级缓存 singletonFactories：
  存放 ObjectFactory。
  等别人真的需要 A 的早期引用时，再决定返回原始 A 还是代理 A。
```

所以可以这样理解：

```text
解决普通循环依赖：
  需要提前暴露 A。

解决带 AOP / 事务代理的循环依赖：
  不只是提前暴露 A，
  还要提前暴露“正确的 A”。
  这个正确的 A 可能是代理对象。
```

> [!summary] 最终理解
> Spring 支持循环依赖，确实用到了三级缓存。
>
> 但“循环依赖”本身只要求能提前拿到 A。
>
> “三级缓存”这种设计，尤其是第三级 `singletonFactories`，主要是为了让提前拿到的 A 在 AOP、事务代理、BeanPostProcessor 参与时也仍然是正确对象。

## 先看对象关系：三级缓存和 A/B 依赖链

> [!note] 先分清对象
> 这篇文章里真正要盯住的不是很多类，而是 A、B 两个业务对象和 `DefaultSingletonBeanRegistry` 里的三级缓存。

```text
[对象1] beanFactory = DefaultListableBeanFactory 实例
        它同时也是：
        DefaultListableBeanFactory
          extends AbstractAutowireCapableBeanFactory
          extends AbstractBeanFactory
          extends FactoryBeanRegistrySupport
          extends DefaultSingletonBeanRegistry

        关系：真正执行 getBean() / doCreateBean() / populateBean() 的对象
        作用：创建 A、创建 B，并读写三级缓存

[对象2] A
        关系：A.b -> B
        作用：先被实例化；属性 b 还没填充时，A 的早期引用会先放进三级缓存

[对象3] B
        关系：B.a -> A
        作用：A 填充属性 b 时触发 B 的创建；B 填充属性 a 时又回头找 A

[对象4] singletonObjects
        所属：DefaultSingletonBeanRegistry
        关系：一级缓存
        作用：保存完整创建好的单例 Bean

[对象5] earlySingletonObjects
        所属：DefaultSingletonBeanRegistry
        关系：二级缓存
        作用：保存已经提前暴露出来的早期 Bean 引用

[对象6] singletonFactories
        所属：DefaultSingletonBeanRegistry
        关系：三级缓存
        作用：保存 ObjectFactory，需要时生成早期 Bean 引用

[对象7] ObjectFactory
        创建位置：addSingletonFactory(beanName, () -> getEarlyBeanReference(...))
        关系：放在 singletonFactories 里
        作用：在别人需要这个 Bean 的早期引用时，调用 getObject() 生成早期引用

[对象8] RuntimeBeanReference
        来源：XML <property ref="..."> 被解析后的引用对象
        关系：A.b = RuntimeBeanReference("b")，B.a = RuntimeBeanReference("a")
        作用：属性填充时触发 getBean(refName)
```

一句话概括：

```text
A 创建到一半时，先把“可以拿到 A 早期引用的 ObjectFactory”放进三级缓存。
B 创建时回头依赖 A，就通过 getSingleton("a") 从三级缓存拿到 A 的早期引用。
B 创建完成后，A 再继续注入完整 B，最终 A 和 B 都进入一级缓存。
```

## 全局导图：A 创建到 B 回头找 A 的完整链路

> [!note] 本篇只追一个问题
> A 正在创建时还不是完整 Bean，为什么 B 还能拿到 A 并完成属性注入。

```text
[00] 外部触发 beanFactory::getBean("a")
     │
     └─ [01] beanFactory::doGetBean("a")
        方法定义在：AbstractBeanFactory
        作用：查缓存、拿 BeanDefinition、决定创建 A
        │
        └─ [02] beanFactory::createBean("a")
           方法定义在：AbstractAutowireCapableBeanFactory
           作用：进入 A 的创建流程
           │
           └─ [03] beanFactory::doCreateBean("a")
              方法定义在：AbstractAutowireCapableBeanFactory
              作用：A 的单个 Bean 创建主流程
              │
              ├─ [03.1] createBeanInstance("a")
              │        作用：A 先实例化，但属性 b 还没注入
              │
              ├─ [03.2] addSingletonFactory("a", ObjectFactory)
              │        方法定义在：DefaultSingletonBeanRegistry
              │        作用：把 A 的早期引用工厂放入 singletonFactories
              │        │
              │        └─ [03.2.1] getEarlyBeanReference("a", mbd, a)
              │                 作用：三级缓存里的 ObjectFactory 真正执行时，会走这里生成 A 的早期引用
              │
              └─ [03.3] populateBean("a")
                 作用：开始给 A 填充属性 b
                 │
                 └─ [04] applyPropertyValues("a")
                    作用：解析 A 的 PropertyValues
                    │
                    └─ [05] resolveValueIfNecessary(RuntimeBeanReference("b"))
                       作用：发现 A.b 引用了 b
                       │
                       └─ [06] beanFactory::getBean("b")
                          作用：为了给 A.b 赋值，开始创建 B
                          │
                          └─ [07] beanFactory::doCreateBean("b")
                             作用：B 的单个 Bean 创建主流程
                             │
                             ├─ [07.1] createBeanInstance("b")
                             │        作用：B 先实例化，但属性 a 还没注入
                             │
                             ├─ [07.2] addSingletonFactory("b", ObjectFactory)
                             │        作用：把 B 的早期引用工厂放入 singletonFactories
                             │
                             └─ [07.3] populateBean("b")
                                作用：开始给 B 填充属性 a
                                │
                                └─ [08] resolveValueIfNecessary(RuntimeBeanReference("a"))
                                   作用：发现 B.a 又引用 a
                                   │
                                   └─ [09] beanFactory::getBean("a")
                                      作用：B 回头找正在创建中的 A
                                      │
                                      └─ [10] getSingleton("a", true)
                                         方法定义在：DefaultSingletonBeanRegistry
                                         作用：从三级缓存体系中拿 A 的早期引用
                                         │
                                         ├─ [10.1] 查 singletonObjects
                                         │        结果：A 还没完整创建，查不到
                                         │
                                         ├─ [10.2] 查 earlySingletonObjects
                                         │        结果：第一次通常还没有
                                         │
                                         └─ [10.3] 查 singletonFactories
                                            结果：找到 A 的 ObjectFactory
                                            │
                                            └─ 调用 ObjectFactory.getObject()
                                               │
                                               ├─ 执行 getEarlyBeanReference("a", mbd, a)
                                               ├─ 把 A 的早期引用放入 earlySingletonObjects
                                               └─ 从 singletonFactories 删除 A 的 ObjectFactory

[11] B 拿到 A 的早期引用
     │
     └─ B.a 注入完成
        │
        └─ B initializeBean()
           │
           └─ B 完整创建完成，放入 singletonObjects

[12] 回到 A 的 populateBean("a")
     │
     └─ A.b 注入完整 B
        │
        └─ A initializeBean()
           │
           └─ A 完整创建完成，放入 singletonObjects
```

## 正文

### 00 示例工程：A 和 B 互相依赖

我们就用下面两个类进行实践，多个类间依赖也是如此。

A 类

```java
package cn.demo1;

import lombok.Getter;
import lombok.Setter;

@Setter
@Getter
public class A {
    private B b;
}
```

B 类

```java
package cn.demo1;

import lombok.Getter;
import lombok.Setter;

@Setter
@Getter
public class B {
    private A a;
}
```

配置文件 test1.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    <bean id="a" class="cn.demo1.A">
        <property name="b" ref="b"/>
    </bean>

    <bean id="b" class="cn.demo1.B">
        <property name="a" ref="a"/>
    </bean>
</beans>
```

### 00.1 DefaultSingletonBeanRegistry：先认识三级缓存字段

DefaultSingletonBeanRegistry 类中的几个特别重要的属性

```java
// 一级缓存      存放完整Bean对象(实例化+初始化)
private final Map<String, Object> singletonObjects = new ConcurrentHashMap<>(256);

// 三级缓存      存放一个lambda表达式
private final Map<String, ObjectFactory<?>> singletonFactories = new HashMap<>(16);

// 二级缓存      存放一个半成品bean对象(只是实例化还未初始化)，提前暴露
private final Map<String, Object> earlySingletonObjects = new ConcurrentHashMap<>(16);
```

> [!note] 位置提醒
> 循环依赖问题通常暴露在属性填充阶段。
> 但是解决它的准备动作，发生在属性填充之前：`doCreateBean()` 会先调用 `addSingletonFactory()`，把当前 Bean 的早期引用工厂放进三级缓存。

### 01-03 A 第一次创建：实例化后提前放入三级缓存

doCreateBean 这个方法

> 可以对照 [[4、依赖注入(DI)#04.1 AbstractAutowireCapableBeanFactory：createBeanInstance 创建 Java 对象|createBeanInstance]] 查看 Spring 是怎么实例化的。

```java
protected Object doCreateBean(String beanName, RootBeanDefinition mbd, @Nullable Object[] args)
			throws BeanCreationException {
    // bean的包装类
    BeanWrapper instanceWrapper = null;
    if (mbd.isSingleton()) {
        instanceWrapper = this.factoryBeanInstanceCache.remove(beanName);
    }
    if (instanceWrapper == null) {
        // 就只是bean的实例化
        instanceWrapper = createBeanInstance(beanName, mbd, args);
    }
    Object bean = instanceWrapper.getWrappedInstance();
    Class<?> beanType = instanceWrapper.getWrappedClass();
    if (beanType != NullBean.class) {
        mbd.resolvedTargetType = beanType;
    }

    // 一般为true
    boolean earlySingletonExposure = (mbd.isSingleton() && this.allowCircularReferences &&isSingletonCurrentlyInCreation(beanName));
    // ....省略部分
    if (earlySingletonExposure) {

     // 这里是将一段lambda放入三级缓存中，可以看见bean填充属性之前会将三级缓存创建好，它传入了一个还未初始化的bean
        addSingletonFactory(beanName, () -> getEarlyBeanReference(beanName, mbd, bean));
    }
    Object exposedObject = bean;
    try {
        populateBean(beanName, mbd, instanceWrapper);
        exposedObject = initializeBean(beanName, exposedObject, mbd);
    }
    // ..........省略部分
    return exposedObject;
}
```

### 03.2 DefaultSingletonBeanRegistry：addSingletonFactory 放入三级缓存

addSingletonFactory

```java
// 其中singletonFactory是一个lambda表达式
protected void addSingletonFactory(String beanName, ObjectFactory<?> singletonFactory) {
    Assert.notNull(singletonFactory, "Singleton factory must not be null");
    synchronized (this.singletonObjects) {
        // 如果我们一级缓存中不存在这个叫beanName的bean
        if (!this.singletonObjects.containsKey(beanName)) {
            // 放入三级缓存中
            this.singletonFactories.put(beanName, singletonFactory);
            // 把二级缓存中叫beanName的半成品bean删除
            this.earlySingletonObjects.remove(beanName);
            // 标记当前注册的bean
            this.registeredSingletons.add(beanName);
        }
    }
}
```

### 03.2.1 getEarlyBeanReference：三级缓存里的 ObjectFactory 会返回什么

lambda 所执行的方法

```java
protected Object getEarlyBeanReference(String beanName, RootBeanDefinition mbd, Object bean) {
    Object exposedObject = bean;
    // 普通bean是进不来的
    if (!mbd.isSynthetic() && hasInstantiationAwareBeanPostProcessors()) {
        for (SmartInstantiationAwareBeanPostProcessor bp : getBeanPostProcessorCache().smartInstantiationAware) {
            exposedObject = bp.getEarlyBeanReference(exposedObject, beanName);
        }
    }
    // 直接返回传进来的bean，返回的是一个还未初始化的bean，是提前暴露的
    return exposedObject;
}
```

### 03.3-05 A 属性填充：解析 RuntimeBeanReference("b")

populateBean 中有调用了 applyPropertyValues 这个方法，具体详情可以对照 [[4、依赖注入(DI)#04.4-04.4.5 AbstractAutowireCapableBeanFactory：populateBean 到 applyPropertyValues|populateBean 到 applyPropertyValues]]。

```java
protected void applyPropertyValues(String beanName, BeanDefinition mbd, BeanWrapper bw, PropertyValues pvs) {

    // Create a deep copy, resolving any references for values.
    List<PropertyValue> deepCopy = new ArrayList<>(original.size());
    boolean resolveNecessary = false;
    for (PropertyValue pv : original) {
        if (pv.isConverted()) {
            deepCopy.add(pv);
        }
        else {
            // 属性名字
            String propertyName = pv.getName();
            //当你引用另一个bean的时候，会把它封装成RuntimeBeanReference这个对象，便于操作
            Object originalValue = pv.getValue();
            // 这里是解析的工作，也就是会产生循环依赖产生的地方
            Object resolvedValue = valueResolver.resolveValueIfNecessary(pv, originalValue);
            // 省略....
        }
}
```

### 05.1 BeanDefinitionValueResolver：resolveValueIfNecessary 识别 RuntimeBeanReference

applyPropertyValues 中有个重要的方法调用，省略无关代码

```java
// 我们当前需要要的就是
public Object resolveValueIfNecessary(Object argName, @Nullable Object value) {
    // 当前bean的属性值的类型正是这个
   if (value instanceof RuntimeBeanReference) {
      RuntimeBeanReference ref = (RuntimeBeanReference) value;
      return resolveReference(argName, ref);
   }
    // 省略...
}
```

### 05.2 BeanDefinitionValueResolver：resolveReference 递归 getBean("b")

resolveReferance 中有一段代码

```java
// 这个方法会调用getBean
@Nullable
private Object resolveReference(Object argName, RuntimeBeanReference ref) {
    // 省略...

    // 上面一般进不去，直接看这个重点
    resolvedName = String.valueOf(doEvaluate(ref.getBeanName()));
    // 获取所依赖的bean
    bean = this.beanFactory.getBean(resolvedName);

    // 省略...

}
```

### 06-08 B 创建过程：B 又依赖回 A

当 A 解析属性 `b` 时，`resolveReference()` 会调用 `getBean("b")`。这会触发 B 的创建。B 的创建过程和 A 一样，也会经历 `doCreateBean()`、`createBeanInstance()`、`addSingletonFactory()`、`populateBean()`。

真正形成闭环的位置是：B 在填充属性 `a` 时，又会通过 `RuntimeBeanReference("a")` 调用 `getBean("a")`。

getBean 从而到这个 doGetBean 方法，其他代码不多说，最主要是下面这个

其中有一段代码，首先它会尝试从缓存中获取到 bean，如果获取不到就创建这个 bean

```java
Object sharedInstance = getSingleton(beanName);
```

### 09-10 getSingleton：B 回头找 A 时读取三级缓存

> 获取缓存 bean 的顺序是，先从一级缓存中取，若不存在，从二级缓存中取，若还是不存在，则从三级缓存中取

```java
@Nullable
protected Object getSingleton(String beanName, boolean allowEarlyReference) {
    // 一级缓存中是否存在
    Object singletonObject = this.singletonObjects.get(beanName);
    // 如果想要获取的bean正在创建中且无一级缓存
    if (singletonObject == null && isSingletonCurrentlyInCreation(beanName)) {
        // 尝试二级缓存
        singletonObject = this.earlySingletonObjects.get(beanName);
        if (singletonObject == null && allowEarlyReference) {
            synchronized (this.singletonObjects) {
                singletonObject = this.singletonObjects.get(beanName);
                if (singletonObject == null) {
                    // 获取二级缓存
                    singletonObject = this.earlySingletonObjects.get(beanName);
                    if (singletonObject == null) {
                        // 获取三级缓存
                        ObjectFactory<?> singletonFactory = this.singletonFactories.get(beanName);
                        if (singletonFactory != null) {
                            // 调用三级缓存，这个地方就会调用我们的lambda表达式了
                            /**
                             *() -> getEarlyBeanReference(beanName, mbd, bean)
                             *这里就是我们解决办法的地方，因为所有普通的bean会首先提前进行三级缓存
                             *所以这里会获取到还未初始化的bean，从而赋值到所依赖当前singletonObject对象的bean
                             */
                            singletonObject = singletonFactory.getObject();
                            // 放入二级缓存中
                            this.earlySingletonObjects.put(beanName, singletonObject);
                            // 三级缓存中移除当前beanName的lambda表达式
                            this.singletonFactories.remove(beanName);
                        }
                    }
                }
            }
        }
    }
    // 完整对象或者还未初始化的对象
    return singletonObject;
}
```

### 11-12 链路回收：B 完整创建后，再回到 A 完成注入

最后经历这个就获取到一个半成品对象所依赖的一个完整对象，然后再将完整对象注入半成品对象中。

> [!note] 更准确地理解这句话
> B 填充属性 `a` 时，拿到的是 A 的早期引用，不是完整初始化后的 A。
> 这个早期引用足够让 B.a 先完成注入，然后 B 可以继续初始化完成。
> 等 B 完整创建完成后，A 的属性 b 就能注入完整 B，A 再继续初始化完成。

### 这一篇最终要记住什么

> [!tip] 最小记忆链路
> A 实例化后，先把 A 的 `ObjectFactory` 放进三级缓存。
> A 填充 b 时触发 B 创建。
> B 填充 a 时回头找 A。
> `getSingleton("a", true)` 从三级缓存拿到 A 的早期引用，并放入二级缓存。
> B 先完成创建，A 再继续完成创建。

### 流程图辅助：原文历程

> 该历程仅代表当前这个项目工程

![image](https://fastly.jsdelivr.net/gh/doocs/source-code-hunter@main/images/spring/循环依赖.png)
